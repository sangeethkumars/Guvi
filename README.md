#include<iostream>
using namespace std;
int main()
{
    int n;
    cin>>n;
    if(n==0)
      cout<<"zero";
    else if(n<0)
      cout<<"negatine number";
    else
      cout<<"positive number";
    return 0;
  }
